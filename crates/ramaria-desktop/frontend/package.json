{
  "name": "ramaria-desktop-frontend",
  "version": "1.6.0",
  "private": true,
  "description": "Ramaria 桌面端前端（Tauri Webview）— 含前端纯逻辑测试（node --test，v1.6 M1 T-V16-1-011）",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "test:watch": "node --test --watch \"tests/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  }
}