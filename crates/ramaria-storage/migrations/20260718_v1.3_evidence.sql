-- =========================================================
-- v1.3: memory_l1 新增 evidence_notes 列
--
-- 用途: 存储 L1 摘要的证据片段（具体的事实性引用），
-- 支撑 L2 事件提取的证据互证判断和溯源链展示。
-- 每条 evidence 记录"谁在什么条件下表达了什么态度/经历了什么事件"。
--
-- 设计说明:
-- - 存储为 TEXT（JSON 数组字符串），如
--   `["用户表示最近一个月每天加班到10点以后", "用户说'感觉身体被掏空了'"]`
-- - NULL 表示尚未生成 evidence_notes（存量数据兼容）
-- - 空数组 `[]` 表示 LLM 未产出有效 evidence（降级路径）
-- =========================================================

ALTER TABLE memory_l1 ADD COLUMN evidence_notes TEXT;
